## 1. 基本思想

Kinakoは、既存言語が抱える「安全性」「複雑性」「学習コスト」の問題を軽減することを目的として設計されたプログラミング言語である。

設計において、次の原則を重視する。

### 1.1 単一責務

一つの概念は一つの責務のみを持つ。

型・権利・実装戦略・実装契約・分類などの異なる概念を分離し、それぞれが独立した責務を持つよう設計する。

### 1.2 最小概念

新しい概念は必要最小限に留める。

既存の概念の組み合わせで表現できるものは、新たな言語機能として導入しない。

### 1.3 明示的な責務

値の構造、アクセス権、実装戦略など、それぞれ異なる役割を明確に分離する。

暗黙に複数の意味を持つ構造は避け、それぞれ独立した概念として扱う。

### 1.4 単純な規則

利用者が理解しやすく説明しやすい規則を優先する。

複雑な特例や例外的な仕様よりも、少数の一貫した規則によって言語全体を構成する。

### 1.5 安全性

危険な操作は可能な限りコンパイル時に検出する。

また、安全性を実現するために複雑な概念を利用者へ要求するのではなく、単純な規則の組み合わせによって実現する。

### 1.6 実用性

安全性だけでなく、実際の開発における記述量・可読性・保守性も重視する。

頻繁に発生する煩雑な記述は、言語仕様によって自然に解消することを目指す。

## 2. プログラムモデル

### 2.1 Value

`Value` とはスタック上、ヒープ上、またはその他のメモリ上に実際に存在するデータである。

### 2.2 Contract

`Contract`とは`Value`, `Right`, `Policy`の総称である。

### 2.2.1 Type

`Type` とは `Value` のフォーマットであり、その値がどのようなデータ構造を持ち、どのような操作（演算）が可能かを定義するもの。

#### プリミティブ型

整数型: `int`

> Note: 常に`32bit`長。

浮動小数点型: `float`

> Note: 常に`32bit`長。\
> Note: NanやInfinityでないことを保証しない。

真偽型: `bool`

> Note: `True`は`1`, `False`は`0`に対応する。

### ジェネリック型

変数有効アドレス値: `ptr`

> Note: この型は内容自体が有効であることを保証する。

固定長配列値: `array`

### 2.2.2 Right

`Variable`への`Binding`を制御するものである。

### 2.2.3 Policy

`Variable`

### 2.3 Variable

`Variable` とは `Value`と`Contract`を通じてプログラム上に読みだすことである。


### 2.4 Binding
<!-- 束縛に関する内容を書く。 -->
`Binding`とは`Value`と`Variable`とのつながりの呼称である。\
`Binding`された`Value`は必ず1以上の`Binding`を持ち、`Binding`されていない`Value`は存在することが許されない。

## 3. Interface

`Interface`とは

## 4. Struct

## 5. Class

## 6. Group

## 7. Group属性群
エイリアス型

Groupから生成することのできるエイリアスである。

### 7.1 Union

Groupから生成することのできるタグ無し列挙エイリアスである。

### 7.2 Dynamic

Groupから生成することのできるvtableなどを用いるタグあり列挙エイリアスである。

## 8. 宣言

## 9. 式

## 10. 制御構文

## 11. Checker Rules